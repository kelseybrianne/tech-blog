const editPost = async (event) => {
    event.preventDefault();
  
    const updatedTitle = document.querySelector("#username-login").value.trim();
    const updatedPostContent = document.querySelector("#username-login").value.trim();
}